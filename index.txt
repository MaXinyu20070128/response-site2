<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS 正方体</title>
    <style>
        body {
            background: #2b3b4e;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .scene {
            width: 300px;
            height: 300px;
            perspective: 800px;
        }
        .cube {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            animation: rotate 12s infinite linear;
        }
        .face {
            position: absolute;
            width: 100%;
            height: 100%;
            border: 2px solid white;
            font-size: 2rem;
            font-weight: bold;
            text-align: center;
            line-height: 300px;
            color: white;
            background: rgba(0,0,0,0.7);
        }
        .front  { transform: translateZ(150px); background: rgba(255,99,71,0.85); }
        .back   { transform: translateZ(-150px) rotateY(180deg); background: rgba(60,179,113,0.85); }
        .left   { transform: translateX(-150px) rotateY(-90deg); background: rgba(70,130,200,0.85); }
        .right  { transform: translateX(150px) rotateY(90deg); background: rgba(255,215,0,0.85); }
        .top    { transform: translateY(-150px) rotateX(90deg); background: rgba(186,85,211,0.85); }
        .bottom { transform: translateY(150px) rotateX(-90deg); background: rgba(255,140,0,0.85); }
        @keyframes rotate {
            0% { transform: rotateX(0deg) rotateY(0deg); }
            100% { transform: rotateX(360deg) rotateY(360deg); }
        }
    </style>
</head>
<body>
    <div class="scene">
        <div class="cube">
            <div class="face front"></div>
            <div class="face back"></div>
            <div class="face left"></div>
            <div class="face right"></div>
            <div class="face top"></div>
            <div class="face bottom"></div>
        </div>
    </div>
</body>
</html>